/*
#include <bits/stdc++.h>

#define ll long long
using namespace std;

int main() {
    ll x, y;
    cin >> x >> y;
    ll count = 0;
    for (int i = 0; i <= count; ++i) {
        x = 3 * x + x;
        y = 2 * y + y;
        if (x > y) {
            count++;
            break;
        } else {
            count++;
        }

    }
    cout << count;

}*/
